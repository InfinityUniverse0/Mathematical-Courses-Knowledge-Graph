function extractRelations() {
    const inputText = document.getElementById('relationInput').value;
    // 关系抽取的逻辑处理代码
    // 可以使用后端服务或API来进行关系抽取
    // 这里只是一个简化示例
    alert(`抽取到的关系：${inputText}`);

}
